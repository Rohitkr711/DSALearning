function reverseANumber(num) {

// M-1 : Iterative method

    // let mulVal = 0;
    // while (num > 0) {
    //     let rem = num % 10;
    //     mulVal = rem + (mulVal * 10);
    //     num = Math.floor(num / 10);

    // }
    // return mulVal;

// M-2 : Using Recursion
if(num==0){
    return
}

}

console.log(reverseANumber(123));
